# Project C01

There were 9 exercises, from 00 to 08. I did from 00 to 07

#### Exercise 00 : ft_ft
Files to turn in : `ft_ft.c` .Allowed functions : `None`. <br>
Create a function that takes a `pointer` to int as a parameter, and sets the value "42"
to that int. <br>
Function Prototype:
```c
void ft_ft(int *nbr);
```
[ex00/ft_ft.c](ex00/ft_ft.c)

#### Exercise 01 : ft_ultimate_ft
Files to turn in : `ft_ultimate_ft.c`. Allowed functions : `None`. <br>
Create a function that takes a pointer to pointer to pointer to pointer to pointer
to pointer to pointer to pointer to pointer to int as a parameter and sets the value
"42" to that int.<br>
Function Prototype:
```c
void ft_ultimate_ft(int *********nbr);
```
[ex01/ft_ultimate_ft.c](ex01/ft_ultimate_ft.c)

#### Exercise 02 : ft_swap
Files to turn in : `ft_swap.c`. Allowed functions : `None`. <br>
Create a function that swaps the value of two integers whose addresses are entered
as parameters.<br>
Function Prototype:
```c
void ft_swap(int *a, int *b);
```
[ex02/ft_swap.c](ex02/ft_swap.c)

#### Exercise 03 : ft_div_mod
Files to turn in : `ft_div_mod.c`. Allowed functions : `None`. <br>
Create a function that divides parameters a by b and stores the result in the int pointed by
div. It also stores the remainder of the division of a by b in the int pointed by mod. <br>
Function Prototype:
```c
void ft_div_mod(int a, int b, int *div, int *mod);
```
[ex03/ft_div_mod.c](ex03/ft_div_mod.c)

#### Exercise 04 : ft_ultimate_div_mod
Files to turn in : `ft_ultimate_div_mod.c`. Allowed functions : `None`. <br>
Create a function that divides parameters a by b. The result of this division is stored in the
int pointed by a. The remainder of the division is stored in the int pointed by b. <br>
Function Prototype:
```c
void ft_ultimate_div_mod(int *a, int *b);
```
[ex04/ft_ultimate_div_mod.c](ex04/ft_ultimate_div_mod.c)

#### Exercise 05 : ft_putstr
Files to turn in : `ft_putstr.c`. Allowed functions : `write`. <br>
Create a function that displays a string of characters on the standard output.<br>
Function Prototype:
```c
void ft_putstr(char *str);
```
[ex05/ft_putstr.c](ex05/ft_putstr.c)

#### Exercise 06 : ft_strlen
Files to turn in : `ft_strlen.c` . Allowed functions : `None`. <br>
Create a function that counts and returns the number of characters in a string.<br>
Function Prototype:
```c
int ft_strlen(char *str);
```
[ex06/ft_strlen.c](ex06/ft_strlen.c)

#### Exercise 07 : ft_rev_int_tab
Files to turn in : `ft_rev_int_tab.c`. Allowed functions : `None`. <br>
Create a function which reverses a given array of integer (first goes last, etc).
The arguments are a pointer to int and the number of ints in the array. <br>
Function Prototype:
```c
void ft_rev_int_tab(int *tab, int size);
```
[ex07/ft_rev_int_tab.c](ex07/ft_rev_int_tab.c)

### Final Grade

![image](https://github.com/user-attachments/assets/6e0369ab-dac2-4f5a-8658-e8a0fae6a4bf)

![image](https://github.com/user-attachments/assets/fe4cd416-a09f-4834-bbd1-18d753b658ea)
