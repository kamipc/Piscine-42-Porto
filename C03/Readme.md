# Project C03

There were 6 exercises, from 00 to 05. All related to making your own version of a function that already exists.<br>
I tried all but 05 I had given up on it after some tries.

#### Exercise 00 : ft_strcmp
Files to turn in : `ft_strcmp.c`. Allowed functions : `None`. <br>
Reproduce the behavior of the function strcmp (`man strcmp`).<br>
Function prototype:
```c
int ft_strcmp(char *s1, char *s2);
```
[ex00/ft_strcmp.c](ex00/ft_strcmp.c)

#### Exercise 01 : ft_strncmp
Files to turn in : `ft_strncmp.c`. Allowed functions : `None`. <br>
Reproduce the behavior of the function strncmp (`man strncmp`).<br>
Function prototype:
```c
int ft_strncmp(char *s1, char *s2, unsigned int n);
```
[ex01/ft_strncmp.c](ex01/ft_strncmp.c)

#### Exercise 02 : ft_strcat
Files to turn in : `ft_strcat.c`. Allowed functions : `None`. <br>
Reproduce the behavior of the function strcat (`man strcat`).<br>
Function prototype:
```c
char *ft_strcat(char *dest, char *src);
```
[ex01/ft_strcat.c](ex01/ft_strcat.c)

#### Exercise 03 : ft_strncat
Files to turn in : `ft_strncat.c`. Allowed functions : `None`. <br>
Reproduce the behavior of the function strncat (`man strncat`). <br>
Function prototype:
```c
char *ft_strncat(char *dest, char *src, unsigned int nb);
```
[ex03/ft_strncat.c](ex03/ft_strncat.c)

#### Exercise 04 : ft_strstr
Files to turn in : `ft_strstr.c`. Allowed functions : `None`.<br>
Reproduce the behavior of the function strstr (`man strstr`).<br>
Function prototype:
```c
char *ft_strstr(char *str, char *to_find);
```
[ex04/ft_strstr.c](ex04/ft_strstr.c)

#### Exercise 05 : ft_strlcat
Files to turn in : `ft_strlcat.c`. Allowed functions : `None`. <br>
Reproduce the behavior of the function strlcat (`man strlcat`).<br>
Function prototype:
```c
unsigned int ft_strlcat(char *dest, char *src, unsigned int size);
```
[ex05/ft_strlcat.c](ex05/ft_strlcat.c)

### Final Grade

![image](https://github.com/user-attachments/assets/bca4167b-145f-4db7-a244-15d4c3d1d84f)

![image](https://github.com/user-attachments/assets/5c280d04-9dde-4bcd-8fae-fe7afb788e83)
